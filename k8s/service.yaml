apiVersion: v1
kind: Service
metadata:
  labels:
    app: podcounter
  name: podcounter
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: podcounter
  type: ClusterIP
